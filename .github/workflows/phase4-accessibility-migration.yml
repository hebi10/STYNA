name: Phase 4 Accessibility Migration

on:
  push:
    branches:
      - chore/styna-improvement-phase-4
    paths:
      - .github/workflows/phase4-accessibility-migration.yml
      - scripts/phase4-apply-accessibility.cjs

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chore/styna-improvement-phase-4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply accessibility migration
        run: node scripts/phase4-apply-accessibility.cjs
      - name: Verify diff
        run: |
          git diff --check
          git diff --exit-code --quiet && { echo "No migration changes produced"; exit 1; } || true
      - name: Commit migration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app
          git commit -m "feat: improve phase 4 accessibility states"
          git push origin HEAD:chore/styna-improvement-phase-4
